spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  mvc:
    view:
      prefix: /
      suffix: .jsp

server:
  port: 8080



